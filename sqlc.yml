version: "2"
sql:
  - engine: "postgresql"
    queries: "services/db/queries/"
    schema: "services/db/migrations/"
    gen:
      go:
        package: "db"
        sql_package: "pgx/v5"
        out: "services/api/internal/db"
